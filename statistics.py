def sum(values):
  sum=0
  for i int values:
    sum=sum+i
  return sum


def mean(values):
  number_of_elem=len(values)
  sum=0
  mean=0
  for i int values:
    sum=sum+i
  mean=sum/number_of_elem
  return mean
